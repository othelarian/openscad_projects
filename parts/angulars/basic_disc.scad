//basic disc

// IDEAS ###
// limit size to min for web or 4 studs
// #########

module basic_disc_variation() {
  echo("######################################");
  //
  //
  echo("######################################");
}

module basic_disc_show() {
  //
  //
}

module basic_disc(size=1,det=1,variation="mark_I") {
  //
  det = 12*size*det;
  $fn = det;
  //
  if (variation == "mark_I" || variation == 0) {
    //
    //
  }
  //
}