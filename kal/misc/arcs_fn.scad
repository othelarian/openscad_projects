// arcs_fn.scad ########################

// internal functions ##################

// arc construction:
// - ang -> array[start angle, arc angle]
function arc_output(ang,r,n,o) = [
  for (i=[0:n])
  (let tmp_ang = "")
  [0,0]
];

// API #################################

function arc_2pts(pt1,pt2) =
0
;

